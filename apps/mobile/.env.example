# Copy to .env. Leave blank to auto-detect your dev machine's LAN IP from
# the Expo dev server (works for physical devices + simulators on the same
# network). Set explicitly for a deployed/staging API.
EXPO_PUBLIC_API_URL=
