# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before writing any code.

This project targets SDK 54 specifically (downgraded from 57) for Expo Go compatibility. Don't run `expo install` for a newer SDK without checking with the user first.
